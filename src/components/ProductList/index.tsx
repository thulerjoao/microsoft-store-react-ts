import * as Styled from "./style"

const ProductList = ()=>{
    return(
        <Styled.GamesList>
            <p>Lista os Jogos -</p>
            <div>Carrocel com cards dos jogos</div>
        </Styled.GamesList>
    )}

export default ProductList